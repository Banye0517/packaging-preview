# BoxLab 包装预览器 PRD

## 问题

包装设计师需要把六张独立面稿快速映射到真实比例的包装盒上，并在本地完成旋转预览。旧项目和参考站包含多图分配、刀模拆分、AI 与场景配置，增加了不需要的步骤。

## 用户任务

1. 选择六面盒型、三边封自立袋、内包装1或内包装2。
2. 六面盒型分别上传六张图片；自立袋和内包装2分别上传正面和背面图片；内包装1上传完整UV图片。
3. 查看图片在真实比例包装模型上的效果。
4. 调整工艺、盒型或袋型和相机参数。
5. 保存本地项目并导出预览结果。

## 纳入范围

- 独立新应用，不导入旧项目业务模块。
- 仅六面独立上传，支持 PNG、JPG、WebP。
- 真实 Three.js 盒体、鼠标与触控旋转缩放。
- 参数化三边封自立袋：正背片、圆弧截面上/左/右封边、固定白色风琴底、袋体厚度、四角圆角、互斥的无封口/拉链/顶部居中吸嘴。
- 内包装2使用提供的翅中 glTF 原生模型和原生UV；正反面上传后按真实 UV 区域自动满版贴合，独立的缩放、移动、旋转和水平/垂直拉伸收纳在默认折叠的高级调整中。
- 四个设置标签：贴图、工艺、盒型、相机。
- 五种表面工艺同时支持六面盒六个面和自立袋正背两个印刷面；两种包装的工艺数据独立保存。
- 浏览器本地资源、项目文件和图片导出。

## 排除范围

- 场景标签及场景纸张、HDRI、背景、阴影、反射和场景预设控件。
- AI 生图、自动识别、自动分配、多图上传和刀模/PDF 拆分。
- 服务器上传、账号系统和云端项目。

## 安全

- 图片仅在浏览器内存或本地 IndexedDB 中处理。
- 拒绝空文件、非 PNG/JPG/WebP 文件和超过 4000 万像素的图片。
- 无效文件不得覆盖当前有效面稿。

## 验收标准

- 桌面和 390×844 视口都显示且仅显示四个设置标签。
- 六面盒型提供六个具名上传入口；自立袋只提供正面和背面两个入口，任一图片可独立替换和移除。
- 六面材质顺序与 Three.js `BoxGeometry` 六组面一致。
- 盒型尺寸改变时只改变几何比例，不拉伸上传图。
- 圆角毫米数值实时改变盒体边角，六面贴图继续保持独立。
- 自立袋厚度实时改变正背片距离和三边圆弧封边深度；底部保持白色撑开风琴结构。
- 自立袋四角圆角开关和无封口、拉链、顶部居中吸嘴互斥选项实时改变模型。
- 自立袋正面和背面可独立上传烫金、烫银、镭射、UV亮膜、击凸/压凹蒙版；封边、结构面和风琴底不接收工艺。
- 内包装2只有正面、背面两个上传入口；左UV岛为正面、右UV岛为背面；两面变换独立且图片保持宽高比。
- 预览中的每个包装模型保留贴近产品底部的真实接触投影；预览区不出现独立于模型的大椭圆背景阴影。
- 导出菜单固定提供四个选项：普通 PNG（无投影）、普通 PNG（有投影）、高清 PNG（无投影）、高清 PNG（有投影）。普通 PNG 为 800×800，高清 PNG 为 3000×3000；四种文件均为透明背景，带投影版本只额外保留产品接触投影。
- 无“场景”或“AI”入口。
- 测试、类型检查、Lint、生产构建与真实浏览器视觉检查通过。
