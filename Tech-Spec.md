# BoxLab 包装预览器 Tech Spec

## 架构

应用为独立 Vite + React + TypeScript 工程。`ProjectState version: 12` 同时保存六面盒型、自立袋、内包装1、内包装2、悬挂抽纸及盒装/袋装独立工艺状态，`packagingType` 决定当前面板和模型。上传图片转换为浏览器本地 Data URL，供缩略图、Three.js 纹理和本地项目文件共同使用，不发往服务器。旧版本项目加载时补齐后续版本新增的默认状态。

## 六面合同

逻辑面顺序为 `top / left / front / right / back / bottom`。Three.js `BoxGeometry` 材质组顺序固定为 `right / left / top / bottom / front / back`，由自动化测试保护。

每个 `ArtworkAsset` 保存资源 ID、文件名、MIME、像素宽高和本地 Data URL。每个面拥有独立纹理生命周期，替换一个面不会销毁其他面的纹理；校验失败时保留旧资源。

## 状态与动作

- `face/set`：替换指定面。
- `face/remove`：清除指定面。
- `box/set`：更新一个有效数字尺寸。
- `camera/autoRotate`：切换自动旋转。
- 历史层支持 `commit / undo / redo / reset / load`。

## 3D

使用 React Three Fiber 渲染六材质 `RoundedBoxGeometry`。盒高归一化为场景单位，宽、深和圆角按同一毫米比例换算；尺寸或圆角变化时重建并释放几何体。相机极角允许从顶部连续旋转到盒底，适合视图、正视图和重置视图使用三个不同相机位置。固定白棚灯光属于渲染基础设施，不形成可见“场景”功能。

自立袋由 `pouchGeometry.ts` 生成高细分正背曲面、上/左/右三条胶囊形圆弧封边和白色底部风琴面。袋体厚度控制正背片距离和封边截面深度；底部展开深度只控制风琴最大展开量。拉链使用前后横向凸条，吸嘴使用顶部居中短颈与旋盖，三种封口状态互斥。左右封边不是可贴图侧面，底部不生成封死横边。

盒装和袋装工艺共用五种材质、蒙版解析与参数计算。袋装工艺状态仅包含 `front`、`back`，覆盖层复用自立袋分区后的正背几何；结构网格、封边和风琴底不进入覆盖层。蒙版变换只改变覆盖轮廓，金属和镭射膜纹理固定在面板 UV 坐标中。

内包装2加载 `public/models/inner-packaging-2.gltf` 的唯一“翅中”网格，保留原生 UV 和深度比例。渲染层按三角形法线区分正反面并计算真实 UV 边界，左侧区域映射正面、右侧区域映射背面；图片默认按区域满版覆盖并应用模型固定方向校正，用户变换值仍保持 100%、0%、0%、0°、100%、100%。每面独立裁切、缩放、移动、旋转和水平/垂直拉伸。版本10项目迁移到版本11时为两面补齐默认拉伸值。

悬挂抽纸加载 `public/models/hanging-tissue.gltf`，使用新供稿的单一可印刷网格 `悬挂抽纸155` 和独立 `纸.1` 节点，不再同时渲染两个重叠包装主体。四个原生 UV 岛按 U 轴顺序为正面、右侧、背面、左侧；运行时以每个岛的三维位置校准面标签，保留原生 `TEXCOORD_0`，不再使用方向相反的法线判面或重建平面 UV。四面图合成到同一张 atlas，岛外保持白色。`纸.1` 通过 `showPulledSheet` 即时显隐。模型根据包围盒水平居中、底部归零，再对齐接触阴影平面；宽高缩放围绕底部锚点进行。版本11项目迁移到版本12时补齐四面素材、六项默认变换、尺寸、方向和抽纸开关。

预览使用一个只贴近模型底部的 `ContactShadows` 组作为产品接触投影；不使用 CSS 伪元素绘制屏幕级背景阴影。导出菜单将预设明确传入场景：普通 PNG 固定为 800×800，高清 PNG 固定为 3000×3000；有投影版本保留接触投影组，无投影版本只在导出期间临时隐藏该组。四种导出均保持透明清屏、像素比 1 和 1:1 相机宽高比。无论编码成功或失败，`try/finally` 都必须恢复渲染器尺寸、像素比、清屏色、场景背景、相机参数和接触投影可见性。下载使用挂载到 DOM 的 Blob URL 链接，延迟撤销 URL，避免浏览器取消下载。

## 输入安全

只接受 `image/png`、`image/jpeg`、`image/webp`。解码后验证非零尺寸与 4000 万像素上限。SVG 和其他可执行或复合格式不进入纹理加载器。

## 验证

Vitest 覆盖壳层、标签边界、六面/两面上传、校验、状态迁移、reducer、材质顺序、盒体圆角、自立袋厚度与三边封几何、互斥封口和透明 PNG 导出状态恢复；Node Test 覆盖 Sites Worker。发布前运行 `typecheck`、`lint`、`test`、`build`、`test:sites`，并在桌面与 390×844 浏览器视口截图验收。
